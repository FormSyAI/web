# 设计约束

- 以 `/aurinova-reference` 已确认的样式为视觉基准；内容迁移优先复用现有布局、组件与交互。
- 保持白底、深色分区、蓝色主动作、少量金色强调；使用现有 token，不恢复旧紫橙配色。
- 保持统一容器、细分隔线、大标题与等宽标签；结构卡片和按钮默认直角，静态内容避免装饰阴影。
- 保留双首屏、黑白双栏、横向卡片、资源区和多列页脚的节奏；新增内容沿用相邻区块规范。
- 中英文保持同等层级，允许自然换行；避免通过缩小字号或固定高度容纳长文案。
- 复用共享导航、页脚和 UI 组件；新增视觉值先检查 `app/globals.css` 与路由样式。
- 保持移动端可读、键盘焦点可见、触控目标充足；轮播和动效遵循减少动态效果偏好。
- 文案迁移依据 [网站内容架构](docs/WEBSITE_CONTENT_ARCHITECTURE.md)；图表示意、产品规划与真实数据明确区分。

详细规范见 [DESIGN.md](DESIGN.md)，样式入口见 [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)。
